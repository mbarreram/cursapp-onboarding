const sb=window.CURSAPP_SUPABASE,auth=window.CURSAPP_ADMIN_AUTH,app=document.getElementById('adminApp'),modal=document.getElementById('adminModal');const $=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const fmt=v=>v?new Date(v).toLocaleString('es-CL'):'—';let tickets=[];async function req(path,options={}){return sb.request(path,options)}function sla(t){if(t.estado==='resuelto')return{txt:'Resuelto',cls:'green'};if(!t.sla_due_at)return{txt:'Sin SLA',cls:'orange'};const ms=new Date(t.sla_due_at)-Date.now();if(ms<=0)return{txt:'SLA vencido',cls:'red'};const h=Math.ceil(ms/36e5);return{txt:`SLA ${h}h`,cls:h<=2?'red':h<=8?'orange':'green'}}async function load(){const[ts,rs]=await Promise.all([req('tickets?select=*&order=created_at.desc'),req('ticket_responses?select=*&order=created_at.asc')]);const map=new Map();(rs||[]).forEach(r=>{const x=map.get(r.ticket_id)||[];x.push(r);map.set(r.ticket_id,x)});tickets=(ts||[]).map(t=>({...t,responses:map.get(t.id)||[]}));const open=tickets.filter(t=>t.estado!=='resuelto').length;const badge=document.getElementById('notifBadge');if(badge){badge.textContent=open?String(open):'';badge.title=`${open} ticket(s) pendiente(s)`}}function title(a,b){$('#viewTitle').textContent=a;$('#viewSub').textContent=b}function close(){modal.innerHTML=''}function open(html){modal.innerHTML=`<div class="modalBg"><div class="modalCard">${html}</div></div>`}function render(){title('Tickets de soporte','Fuente oficial: Supabase');app.innerHTML=`<div class="toolbar stickyToolbar"><input id="stq" placeholder="Buscar ticket..."><select id="sts"><option value="">Todos</option><option>abierto</option><option>revision</option><option>resuelto</option></select></div><section class="panel"><div class="panelHead"><h2>Tickets</h2><span id="stcount" class="badge purple"></span></div><div id="stlist" class="list"></div></section>`;const draw=()=>{const q=($('#stq').value||'').toLowerCase(),s=$('#sts').value;const rows=tickets.filter(t=>(!s||t.estado===s)&&JSON.stringify(t).toLowerCase().includes(q));$('#stcount').textContent=`${rows.length} resultados`;$('#stlist').innerHTML=rows.map(t=>{const z=sla(t);return`<div class="row ticketRow" data-id="${esc(t.id)}" style="cursor:pointer"><div class="rowIcon">🎫</div><div><b>${esc(t.folio||t.id)} · ${esc(t.asunto)}</b><p>${esc(t.colegio_nombre||'Sin colegio')} · ${esc(t.curso_nombre||'Sin curso')} · ${fmt(t.created_at)}</p><small>${t.assigned_admin_id?'Asignado':'Sin asignar'}</small></div><span class="badge ${z.cls}">${z.txt}</span></div>`}).join('')||'<p class="muted">Sin tickets.</p>';document.querySelectorAll('[data-id]').forEach(e=>e.onclick=()=>edit(e.dataset.id))};$('#stq').oninput=draw;$('#sts').onchange=draw;draw()}function edit(id){const t=tickets.find(x=>x.id===id);if(!t)return;const z=sla(t),msgs=t.responses.map(r=>`<div class="ticketMsg"><div><b>${r.author_role==='admin'?'Soporte Cursapp':'Usuario'}</b><span>${fmt(r.created_at)}</span></div><p>${esc(r.body)}</p></div>`).join('')||'<p class="muted">Sin respuestas.</p>';open(`<h2>${esc(t.folio||t.id)} · ${esc(t.asunto)}</h2><p><b>${z.txt}</b> · Responsable: ${t.assigned_admin_id?'Admin asignado':'Sin asignar'}</p><p>${esc(t.descripcion||'')}</p><div class="ticketConversation">${msgs}</div><div class="formGrid"><div><label>Estado</label><select id="ste"><option>abierto</option><option>revision</option><option>resuelto</option></select></div><div><label>Prioridad</label><select id="stp"><option>normal</option><option>alta</option><option>media</option><option>baja</option><option>critica</option></select></div><div style="grid-column:1/-1"><label>Respuesta al usuario</label><textarea id="str"></textarea></div></div><div style="display:flex;gap:10px;justify-content:flex-end"><button class="adminBtn ghost" id="stcancel">Cancelar</button><button class="adminBtn" id="stsave">Guardar y asignarme</button></div>`);$('#ste').value=t.estado||'abierto';$('#stp').value=t.prioridad||'normal';$('#stcancel').onclick=close;$('#stsave').onclick=async()=>{const estado=$('#ste').value,prioridad=$('#stp').value,body=$('#str').value.trim();await req(`tickets?id=eq.${encodeURIComponent(id)}`,{method:'PATCH',body:JSON.stringify({estado,prioridad,assigned_admin_id:auth.user.id,updated_at:new Date().toISOString(),closed_at:estado==='resuelto'?new Date().toISOString():null})});if(body)await req('ticket_responses',{method:'POST',body:JSON.stringify({ticket_id:id,author_id:auth.user.id,author_role:'admin',body})});close();await load();render()}}await load();const old=window.Admin.go.bind(window.Admin);window.Admin.go=tab=>tab==='tickets'?render():old(tab);