const products=[{icon:'🎩',title:'Traje de huaso niño',cat:'Vestuario',school:'Colegio San Pedro',dist:'1,8 km',price:'$18.000',type:'Venta'},{icon:'📚',title:'Libro Matemáticas 4°',cat:'Libros',school:'Colegio Central',dist:'Mi colegio',price:'$8.000',type:'Venta'},{icon:'👕',title:'Polerón escolar talla 12',cat:'Uniformes',school:'Colegio Los Robles',dist:'3,2 km',price:'Intercambio',type:'Intercambio'},{icon:'⚽',title:'Zapatos fútbol niño',cat:'Deportes',school:'Colegio Central',dist:'Mi colegio',price:'$12.000',type:'Venta'},{icon:'🎭',title:'Vestido cueca niña',cat:'Vestuario',school:'Colegio Santa Ana',dist:'2,4 km',price:'$15.000',type:'Venta'}];
function card(p){return `<article class="product" onclick="openDetail('${p.title}')"><div class="pic">${p.icon}</div><div class="body"><b>${p.title}</b><span>${p.school} · ${p.dist}</span><span>${p.cat} · ${p.type}</span><strong>${p.price}</strong></div></article>`}
function render(){document.getElementById('latestList').innerHTML=products.slice(0,3).map(p=>`<div class="featured" onclick="openDetail('${p.title}')"><div class="pic big">${p.icon}</div><div><small>${p.school} · ${p.dist}</small><b>${p.title}</b><p>${p.cat} · ${p.type}</p><strong>${p.price}</strong></div></div>`).join('');document.getElementById('marketList').innerHTML=products.map(card).join('')}
function showView(v){document.querySelectorAll('.view').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.tabs button').forEach(x=>x.classList.toggle('active',x.dataset.view===v));document.getElementById('view-'+v).classList.add('active')}
function filterCat(cat){showView('explorar');document.getElementById('marketList').innerHTML=products.filter(p=>p.cat===cat).map(card).join('')}
function openDetail(title){const p=products.find(x=>x.title===title)||products[0];document.getElementById('modal').innerHTML=`<div class="modal"><h3>${p.icon} ${p.title}</h3><p><b>${p.price}</b></p><p>${p.school} · ${p.dist}</p><p>Apoderado verificado. Cursapp no procesa pagos ni garantiza la transacción.</p><button onclick="toast('Solicitud de contacto enviada')">Contactar / reservar</button><button onclick="toast('Publicación reportada para revisión')">🚩 Reportar</button><button onclick="document.getElementById('modal').innerHTML=''">Cerrar</button></div>`}
function toast(t){const el=document.getElementById('toast');el.textContent=t;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),1800)}
render();